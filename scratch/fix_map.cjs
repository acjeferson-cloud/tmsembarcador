const fs = require('fs'); let code = fs.readFileSync('src/components/Routing/RoutingTower/RoutingMap.tsx', 'utf8'); code = code.replace('const mappedOrders = [\\n      ...pendingOrders.map(o => ({ ...o, isSelected: false })),\\n      ...selectedOrders.map(o => ({ ...o, isSelected: true }))\\n    ];', 'const mappedOrders = Array.from(new Map([\\n      ...pendingOrders.map(o => [o.id || o.order_number, { ...o, isSelected: false }]),\\n      ...selectedOrders.map(o => [o.id || o.order_number, { ...o, isSelected: true }])\\n    ]).values());'); fs.writeFileSync('src/components/Routing/RoutingTower/RoutingMap.tsx', code);
